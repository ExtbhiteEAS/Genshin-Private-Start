Don't touch this folder or delete, because this helping update script.
If you want updating script - you will need start private-script-updater.cmd file. And this is require:
don't rename from "Genshin-Private-Start-main" to other names, because this replacing files.